/// ビルドごとに変わる定数をまとめた場所。
///
/// 画面 (mind_map_screen.dart) と部品の両方から見たいので、
/// 何にも依存しない葉っぱのファイルに置いてある。
library;

/// Microsoft Store 提出用ビルドか。
///
/// ★ ストアの規約では、 ストア外から実行ファイルを取ってきて動かすことが
///   できない。 そのため
///     flutter build windows --release --dart-define=STORE_BUILD=true …
///   で立てて、 次のものを落とす:
///     ・ffmpeg の自動ダウンロード
///     ・Web 自動操作の汎用コマンド実行
///     ・ビデオエディター (動画編集ページ) 一式
///   zip 配布版 (既定) は今までどおり全部動く。
///
/// ビデオエディターを丸ごと隠すのは、 書き出しに ffmpeg.exe が要るのに
/// ストア版では取りに行けないため。 「開けるのに何も出来ない」 より、
/// 最初から無い方が分かりやすい。 既に作ってあるページは消さず、
/// 開いた時に案内を出すだけにしてある (中身は prefs に残る)。
const bool kStoreBuild = bool.fromEnvironment('STORE_BUILD');
